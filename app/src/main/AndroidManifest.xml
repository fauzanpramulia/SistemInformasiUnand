<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fauzanpramulia.sisteminformasiunand">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/si_benar"
        android:label="@string/app_name"
        android:roundIcon="@drawable/si_benar"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".angkatan.SatuActivity"
            android:label="ANGKATAN 10" />
        <activity
            android:name=".activity.SettingsActivity"
            android:label="Settings" />
        <activity android:name=".angkatan.CustumeActivity"></activity>
    </application>

</manifest>